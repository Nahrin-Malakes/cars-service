import { type NextPage } from "next";

const Home: NextPage = () => {
  return (
    <>
      {/* <button color="red" onClick={() => void handleSearch()}></button> */}
      <App />
    </>
  );
};

export default Home;
