import { type NextPage } from "next";
import App from "../App";

const Home: NextPage = () => {
  return (
    <>
      {/* <button color="red" onClick={() => void handleSearch()}></button> */}
      <App />
    </>
  );
};

export default Home;
